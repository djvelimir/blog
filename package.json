{
  "scripts": {
    "build": "hugo --minify",
    "predeploy" : "set HUGO_ENV=production && yarn run build",
    "deploy" : "gh-pages -d public"
  },
  "dependencies": {
    "gh-pages": "^4.0.0"
  }
}
