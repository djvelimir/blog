{
  "scripts": {
    "build": "hugo --minify",
    "predeploy": "export HUGO_ENV=production && yarn run build",
    "deploy": "gh-pages -d public"
  },
  "dependencies": {
    "gh-pages": "^6.1.1"
  }
}
